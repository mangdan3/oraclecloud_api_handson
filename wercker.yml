box: node
build:
  steps:
    - script:
        name: install-expressjs
        code: sudo npm install express@6.5.0 --ignore-scripts --no-optional --global
    - script:
        name: install-dredd
        code: sudo npm install dredd@5.3.2 --ignore-scripts --no-optional --global
    - script:
        name: dredd
        code: dredd
